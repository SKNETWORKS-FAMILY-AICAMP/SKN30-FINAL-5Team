# Infrastructure

로컬 Docker와 MVP 배포 설정의 소유 영역입니다. 현재 단계에는 실행 설정을 만들지 않습니다.

- `docker/`: API와 PostgreSQL 로컬 구성
- `deployment/`: 선택한 플랫폼의 배포 선언과 운영 runbook

Kubernetes, Redis, worker, queue는 기본 구조가 아닙니다.

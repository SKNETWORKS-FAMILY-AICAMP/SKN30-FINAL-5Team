# Local Docker orchestration only. Do not reuse these credentials outside local development.
COMPOSE_PROJECT_NAME=exercise_wellness

# PostgreSQL
POSTGRES_DB=exercise_app
POSTGRES_USER=exercise_app
POSTGRES_PASSWORD=local_dev_only
POSTGRES_PORT=5432

# API
API_PORT=8000

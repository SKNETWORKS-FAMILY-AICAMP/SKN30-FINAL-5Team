# 국민체력100 동영상 메타데이터

공공데이터포털 데이터셋 `15108846`의 출처 계약은 `source.json`에 고정한다.
로컬 수집 결과는 `snapshots/` 아래에 저장되며 기본적으로 Git에 커밋하지 않는다.

수집 endpoint:

- `training-video`: 운동처방동영상 목록
- `standard-fitness`: 생애주기별표준운동 목록
- `routine`: 목적별루틴운동 목록

근골격계운동은 질환·재활 표현을 포함할 수 있어 초기 allowlist에서 제외한다.
API 결과에 포함된 외부 영상/이미지 URL은 참조 메타데이터일 뿐, 해당 미디어의
재배포 허가를 의미하지 않는다.

# Application modules

모듈은 `identity`, `profiles`, `catalog`, `routines`, `checkins`, `decisions`, `workouts`,
`weekly_reports`, `external_context`로 나눕니다. 각 모듈은 public service/port를 통해서만 협업하고
다른 모듈의 repository를 직접 사용하지 않습니다.
